const domain = "http://vue.studyit.io/api";

// 导出 默认值
export default {
    // 轮播图接口
    getLunbo: `${domain}/getlunbo`,
    // 新闻接口
    getNL:`${domain}/getnewslist`,
    getND:`${domain}/getnew/`   // 需要添加 id
}